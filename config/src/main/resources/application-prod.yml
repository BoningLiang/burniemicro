server:
  port: 7001
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: http://localhost:5050/gitlab/liangboning/config-repo.git
          search-paths: /**
